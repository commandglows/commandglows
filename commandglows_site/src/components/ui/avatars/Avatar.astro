---
const { src, alt } = Astro.props

interface Props {
	src: string
	alt: string
}
---

<img
	class="inline-block h-8 w-8 rounded-full ring-2 ring-content-bg-elevated dark:ring-content-bg-strong"
	src={src}
	alt={alt}
	draggable="false"
	loading="eager"
/>
