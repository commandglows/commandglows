---
interface Props {
	id: string
	name: string
	errorId: string
	label?: string
	content?: string
	placeholder?: string
	forgot?: boolean
	forgotText?: string
	autocomplete?: string
	isConfirm?: boolean
	required?: boolean
}

const {
	id,
	name,
	errorId,
	label = 'Password',
	content,
	placeholder = 'Enter your password',
	forgot = false,
	forgotText = 'Forgot password?',
	autocomplete = 'current-password',
	required = false,
} = Astro.props
---

<div>
	<div class="flex items-center justify-between">
		<label
			for={id}
			class="mb-2 block text-content-text-secondary dark:text-content-text-muted"
		>
			{label}
		</label>
		{
			forgot && (
				<a
					href="/signin"
					class="text-sm font-medium text-magenta decoration-2 hover:underline"
				>
					{forgotText}
				</a>
			)
		}
	</div>
	<div class="relative">
		<input
			type="password"
			id={id}
			name={name}
			class="block w-full rounded-lg border border-content-border px-4 py-3 focus:border-magenta focus:ring-magenta dark:border-content-border-strong dark:bg-content-bg-strong dark:text-content-text-muted"
			placeholder={placeholder}
			required={required}
			aria-describedby={errorId}
			autocomplete={autocomplete}
		/>
		<div id={errorId} class="mt-2 hidden text-red" role="alert">
		</div>
	</div>
	{
		content && (
			<p class="mt-2 text-content-text-muted dark:text-content-text-tertiary">
				{content}
			</p>
		)
	}
</div>
